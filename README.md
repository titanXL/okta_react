### POC OKTA Implicit flow - react for SPA & express for backend

Set okta clientId in the server as process.env.clientId={clientId}

Set okta clientId in the client as process.env.REACT_APP_NOT_SECRET_CODE
